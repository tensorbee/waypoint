//! MySQL-specific implementations.
//!
//! Each submodule provides the MySQL implementation of a feature whose
//! engine-agnostic types and dispatcher live at the top level of the crate.

pub mod history;
